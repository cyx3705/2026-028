# 设计：把微信当非信任中间件的端到端加密对话

> 目标：**微信整条链路都按不可信对待**。对话双方通过公私钥协商密钥，
> 密文经由微信传递，网页只是为了让对方 0 安装就能用。

---

## 一、必须先讲的一件事：裸 ECDH 挡不住中间人

这是整个方案里最容易做错、也最致命的点。

**你设想的流程**：双方各生成一对公私钥 → 通过微信交换公钥 → ECDH 协商出共享密钥 → 之后加密对话。

**问题**：微信就在这条路上，它可以**把公钥换掉**。

```
        你以为的
  Alice ──pubA──▶ 微信 ──pubA──▶ Bob
  Alice ◀─pubB─── 微信 ◀─pubB─── Bob
        → 双方算出同一个密钥 K，微信看不到。✅

        实际可能发生的
  Alice ──pubA──▶ 微信 ──pubM──▶ Bob      ← 微信塞了自己的公钥
  Alice ◀─pubM'── 微信 ◀─pubB─── Bob
        → Alice 算出 K1 = ECDH(a, M)
          Bob   算出 K2 = ECDH(b, M)
          微信两个都知道
        → 微信解密、重加密、转发。双方各自都觉得"加密正常"。
```

**结论**：

| 攻击者能力 | 裸 ECDH 够不够 |
|---|---|
| **被动**（只是存日志、事后翻记录） | ✅ 够。它拿不到私钥，解不开 |
| **主动**（能改、能插、能替换消息） | ❌ **完全不够**。微信是通道，它天然有这个能力 |

所以「微信是非信任通道」这个前提本身就意味着**它具备主动能力**——
那么裸 ECDH 是不成立的，必须加**认证**。

---

## 二、认证怎么做（三选一）

| 方案 | 做法 | 代价 | 强度 |
|---|---|---|---|
| **A. SAS 短认证串** ⭐ | ECDH 出共享密钥后，双方各自从会话摘要算出**6 位数字**，**打个电话/当面念一遍**，一样就确认 | 每次新会话多一步，只在这一步用到第二条信道 | 高（Signal 就是这么干的） |
| **B. 预共享口令（PAKE）** | 事先约定一个口令，用 SPAKE2/OPAQUE 之类，口令**不过网**也能认证密钥交换 | 要集成 PAKE，比 A 复杂 | 高，且不用每次比对 |
| **C. 长期身份密钥 + 首次带外核对** | 首次把身份公钥**带外**核对一次（扫码、念指纹），之后靠它认证 | 首次麻烦，之后无感 | 高 |

**我的建议：先做 A。** 6 位数字念一遍，用户能理解，实现量最小，而且是标准做法。

> ⚠️ 注意一个陷阱：**如果网页本身是走微信发过去的，网页里带不了任何秘密**。
> 微信能看到网页内容，所以密钥不能藏在网页里，只能协商。

---

## 三、一个绕不开的拓扑约束

你想要的四件事，**最多只能同时满足三件**：

- ① 微信当中间件
- ② 网页当界面
- ③ 对面 0 安装
- ④ 对面也自动收发（不用手动搬运）

原因很硬：**WeChat 里的密文是一串文本，而浏览器读不到微信的消息。**
必须有个东西在"微信 ↔ 网页"之间搬运。这个东西在**你这边**叫桥，在**对面那边**也得有一个。

| 组合 | 谁搬密文（你这边） | 谁搬密文（对面） | 体验 |
|---|---|---|---|
| **①②③**，放弃④ | 桥自动 | **对面手动复制粘贴** | 你舒服，对面每条消息切一次窗口 |
| **①②④**，放弃③ | 桥自动 | 对面也跑桥（Python + 脚本） | 都舒服，但对面要装东西 |
| **②③④**，放弃① | 你起公网中继 | 什么都不用 | 都舒服，但你要维护公网入口，且不再用微信 |

**这三条路没有第四条。** 你现在想要的是第一行。

---

## 四、建议的 v1 协议（具体到消息格式）

### 密钥

- 曲线：**P-256**（WebCrypto 原生支持，无需引库）；或 X25519（更现代，但 WebCrypto 支持度差些）
- 身份密钥对：生成后存在网页 `localStorage`，长期使用
- 会话密钥：`ECDH(我方私钥, 对方公钥)` → **HKDF-SHA256** → AES-256-GCM 密钥

### 消息格式（要能直接复制粘贴，所以必须短且安全）

全部用 **Base64Url**（无 `+` `/` `=`，粘到哪儿都不会被转义），加个前缀方便识别自己的消息：

```
密钥交换：  E2E1-K.<base64url(公钥)>[:<base64url(SAS摘要)>]
加密消息：  E2E1-M.<base64url(iv || ciphertext)>
```

- 前缀 `E2E1-` 让页面能在一堆聊天记录里认出自己的消息，别的消息直接忽略
- 收到的不是这个格式 → 页面提示"这不是加密消息"

### 防重放 / 防乱序

微信可能重排、重发。所以：

- 明文字段里带 **单调递增计数器 `seq`**（按发送方向各自计数）
- **计数器放进 AES-GCM 的 AAD**，被篡改会直接解密失败
- 收到 `seq` 不大于已见最大值的，直接丢弃

### 防 nonce 重用（这条最容易出人命）

AES-GCM **同一个密钥下 nonce 绝对不能重用**，重用会让攻击者恢复出明文和认证密钥。

不要图省事用随机 IV 就完事，直接用**确定性的**：

```
iv = 方向字节(1B) || seq(11B 大端)     → 正好 12 字节
```

两个方向用不同的方向字节，所以永远不会撞。

---

## 五、必须一起接受的局限

- **长度会泄漏**。密文长度 ≈ 明文长度，微信能看到"你发了一条 200 字的、一条 3 字的"。
  在意的话要**填充到固定档位**（比如 256/1024/4096 字节），代价是消息变长。
- **元数据完全暴露**：谁跟谁、几点、多久聊一次、频率。加密保护不了这些。
- **没有前向保密**（v1）。用的是长期身份密钥，私钥一旦泄漏，**历史消息全部可解**。
  要修得上双棘轮（double ratchet），工程量会大不少。
- **微信文本消息有长度上限**，超长消息必须分片，分片要带 `part/total` 并支持乱序重组。

---

## 六、分期建议

| 阶段 | 内容 | 依赖 | 风险 |
|---|---|---|---|
| **v1** | `crypto.html` 单文件：ECDH + HKDF + AES-GCM + **SAS 校验** + 剪贴板收发。手动搬运密文 | 无 | 低，纯前端，可离线自测 |
| **v2** | 你这边接桥自动收发（复用已有的 `bridge.py`），对面保持手动 | v1 + 桥 | 中（要接微信后端） |
| **v3** | 双棘轮前向保密、填充、分片 | v1 | 中 |

**强烈建议先做 v1，而且先把"两个人各自开一个页面互相发"跑通**——
加密协议的正确性跟微信完全无关，可以先在本地两个浏览器标签页之间验证，
不用碰微信，也不用碰后端。

这样最难的密码学部分被隔离在最小范围里，出问题好定位。

---

## 七、待你拍板

1. **要不要 SAS 校验？** 
   要 → 能防主动中间人（微信主动攻击也读不到），代价是每次新会话念一次数字。
   不要 → 只能防“事后翻日志”，微信想读随时能读。**我建议要。**
2. **对面能接受手动复制粘贴吗？**（即接受第一行拓扑）还是宁愿让对面也装桥？
3. **v1 先不碰微信、只在本地两个标签页之间验证，行吗？** 
   我建议行 —— 先证明加密是对的，再接通道。
4. **要不要前向保密？** 不要的话 v1 会简单很多，但私钥泄漏 = 历史全泄。
