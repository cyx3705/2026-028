# 设计：把微信当不可信管道的端到端加密对话

> 目标：微信**整条链路都按不可信对待**。对话双方通过 ECDH 协商密钥，
> 密文经由微信传递，网页只是为了让对方 0 安装就能用。
>
> 本文描述**实际实现**。后面几节标了「当初的设想 vs 最后怎么做」，
> 因为有几个直觉上很自然的选择，实测下来是错的。

---

## 一、最容易做错的一件事：裸 ECDH 挡不住中间人

**设想**：双方各生成一对公私钥 → 通过微信交换公钥 → ECDH 协商 → 加密对话。

**问题**：微信就在这条路上，它可以**把公钥换掉**。

```
        你以为的
  Alice ──pubA──▶ 微信 ──pubA──▶ Bob
  Alice ◀─pubB─── 微信 ◀─pubB─── Bob
        → 双方算出同一个密钥 K，微信看不到 ✅

        实际可能发生的
  Alice ──pubA──▶ 微信 ──pubM──▶ Bob      ← 微信塞了自己的公钥
  Alice ◀─pubM'── 微信 ◀─pubB─── Bob
        → Alice 算 K1 = ECDH(a, M)
          Bob   算 K2 = ECDH(b, M)
          微信两个都知道 → 解密、重加密、转发
          双方各自都觉得"加密正常"
```

| 攻击者能力 | 裸 ECDH 够不够 |
|---|---|
| **被动**（只存日志、事后翻记录） | ✅ 够。拿不到私钥，解不开 |
| **主动**（能改、能插、能替换消息） | ❌ **完全不够**。微信是通道，它天然有这个能力 |

### 本项目的决定：不做 SAS

按需求「没那么严谨，只需要把密文加密即可，让他事后看起来比较炫酷」，
**v1 没有做 SAS 短认证串**。后果必须说清楚：

> **能防的是被动**（存日志、事后翻记录、服务器留档）。
> **防不住微信主动做中间人**——它换掉公钥就能全程明文。这是已知且被接受的取舍。

要补的话，标准做法是：ECDH 出共享密钥后，双方各自从会话摘要算出 **6 位数字**，
**打个电话/当面念一遍**，一样就确认。Signal 就是这么干的。工作量不大，但每次新会话多一步。

---

## 二、实际协议

### 密钥

| 环节 | 实现 |
|---|---|
| 曲线 | **P-256**（WebCrypto 原生，零依赖） |
| 身份密钥 | 生成后存 `localStorage`，长期使用；**私钥永不出本机** |
| 共享秘密 | `ECDH(我方私钥, 对方公钥)` → 256 bit |
| 密钥派生 | **HKDF-SHA256**，salt = 双方公钥按字典序拼接，info = `"e2e1 v1 session keys"` |
| 输出 | 512 bit，**切成两把独立的 AES-256 密钥** |

**切分规则**：比较双方公钥的字典序，小的一方拿 `k[0..32]` 当发送键，另一方拿 `k[32..64]`。
所以：

```
我的发送键 = 对方的接收键
我的接收键 = 对方的发送键
```

两把钥匙用途不同，方向天然隔离——**不需要在 IV 里塞方向字节**（见下）。

### 消息格式

全部用 **Base64Url**（无 `+` `/` `=`，粘到哪儿都不会被转义）：

```
公钥交换：  E2E1-K.<base64url(公钥)>
加密消息：  E2E1-M.<base64url(iv || ciphertext || tag)>
```

前缀让页面能在一堆聊天记录里认出自己的消息；不是这个格式的直接忽略。

### nonce 与防重放

**AES-GCM 在同一把密钥下重用 nonce 会直接泄漏明文和认证密钥**，这条最容易出人命。

```
iv  = 12 字节大端计数器
AAD = "E2E1-M|" + 计数器
```

- 计数器**单调递增**，确定性，永不重复（不用随机 IV——碰撞概率低但没必要赌）
- 计数器**同时进 AAD**，被篡改就直接认证失败
- 收到 `ctr <= 已见最大值` 的，直接丢弃

> **当初的设想 vs 最后怎么做**
> 设计稿里写的是 `iv = 方向字节(1B) || seq(11B)`。
> 实际实现改成了**两把独立密钥 + 完整 12 字节计数器**——
> 既然两个方向的密钥本来就不同，再用方向字节区分是多余的，
> 而且省下的 1 字节让计数器不容易溢出。
> 计数器也没放进明文字段，而是从 IV 里读出来再拼进 AAD，消息更短。

### 已实测验证

`page/test_loopback.js` 把页面里的 `Crypto` 模块**原样抠出来**跑真 WebCrypto，18/18 通过：

- 我自己的接收键**解不开**我自己的出站密文 → 证明两把密钥确实不同（切分没写错）
- 对方的接收键能解开我的出站密文；对方的发送键加密的回复我能解开
- 重放被拒、篡改被拒（AES-GCM 认证）
- 连续消息 IV 各不相同且单调递增

---

## 三、拓扑：这件事的硬约束

**WeChat 里的密文是一串文本，而浏览器读不到微信的消息。**
所以"微信 ↔ 网页"之间必须有东西搬运。这个东西在**你这边**叫桥，在**对面那边**也得有一个。

设计稿里当时说四件事最多同时满足三件（微信当中间件 / 网页当界面 / 对面 0 安装 / 对面自动收发）。

**最后实际做到的**：①②④ + ③的弱化版。

| | 怎么做到的 |
|---|---|
| ① 微信当中间件 | ✅ 密文就是普通微信消息 |
| ② 网页当界面 | ✅ 单文件 `chat.html` |
| ③ 对面 0 安装 | ⚠️ **不需要装 Python / Node / .NET SDK**，但要跑一次安装器 |
| ④ 对面自动收发 | ✅ 桥监听剪贴板，对方在微信里 `Ctrl+C` 就自动收 |

关键突破是**把桥的源码内嵌进网页**：`chat.html` 里带着 36 KB 的 `bridge.cs`（base64），
对方点一下「下载安装桥」，页面吐出 `.bat`，用 **Windows 自带的 `csc.exe` 现场编译**——
全程不下载任何东西，不需要任何 SDK。

所以"0 安装"的准确含义是：**不用装任何开发工具链**，代价是双击一次安装器。

---

## 四、必须一起接受的局限

| 局限 | 说明 | 想修的话 |
|---|---|---|
| **长度泄漏** | 密文长度 ≈ 明文长度，微信能看到"发了条 200 字的、一条 3 字的" | 填充到固定档位（256/1024/4096），代价是消息变长 |
| **元数据全暴露** | 谁跟谁、几点、多久聊一次、频率。加密保护不了这些 | 无解 |
| **没有前向保密** | 长期身份密钥，私钥一旦泄漏，**历史消息全部可解** | 上双棘轮 |
| **没有 SAS** | 防不住主动中间人（见第一节） | 加 6 位数字比对 |
| **超长消息** | 微信文本消息有长度上限，超长必须分片 | 分片带 `part/total` 并支持乱序重组 |

---

## 五、通道本身不可信到什么程度

这一点决定了上面所有取舍的合理性。**实测结论**：

微信 4.x 主界面是 **Qt 5.15.14 + 自绘渲染**，整个窗口对无障碍层**只暴露 1 个元素**：

```
window: class='Qt51514QWindowIcon' pid=3100 proc=Weixin
UIA tree:
  [Pane] name=Y(len 21) class=MMUIRenderSubWindowHW
total descendants: 1
Text/ListItem elements: 0
```

也就是说：**读不到消息内容**。所以只能靠剪贴板搬运，而这也正是这套方案
**对微信版本免疫**的原因——它不碰协议、不碰界面结构，
只依赖「剪贴板」和「Ctrl+V」这两个十年不变的东西。

同时它也说明：微信官方客户端把消息内容保护得很好，
我们的密文在它眼里就是一串无意义的 Base64Url 文本。
